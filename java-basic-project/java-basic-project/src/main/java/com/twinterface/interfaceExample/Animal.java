package com.twinterface.interfaceExample;
//定义接口 Animal
//接口关键字：interface
//接口内只做函数声明，不用写函数体

public interface Animal {
    public String animalSound(); // interface method (does not have a body)
    public String run(); // interface method (does not have a body)
}
